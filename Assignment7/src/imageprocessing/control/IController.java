package imageprocessing.control;


/**
 * Interface represents the controller that is responsible for communicating with the view
 * and model.
 */
public interface IController {
  /**
   * goController processes inputs and sends data to the model.
   */
  void goController();
}
